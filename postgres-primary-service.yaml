apiVersion: v1
kind: Service
metadata:
  name: postgres-primary
  labels:
    app: postgres-primary
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres-primary