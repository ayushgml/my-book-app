apiVersion: v1
kind: Service
metadata:
  name: postgres-standby
  labels:
    app: postgres-standby
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres-standby